# Database (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://inventory_user:inventory_secret_password@localhost:5432/inventory_db

# For SQLite local development (uncomment to use):
# DATABASE_URL=sqlite+aiosqlite:///./data/inventory.db

# JWT Configuration
JWT_SECRET=change-me-to-a-strong-random-string-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Initial Admin User (created on first startup)
FIRST_ADMIN_EMAIL=admin@example.com
FIRST_ADMIN_PASSWORD=admin123
